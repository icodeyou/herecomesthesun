import 'package:flutter/cupertino.dart';

class MyColors {
  // Primary colors
  static const Color primary = Color(hexPrimary);
  static const Color primaryDark = Color(hexPrimaryDark);
  static const Color primaryLight = Color(hexPrimaryLight);
  static const Color accent = Color(hexAccent);

  static const int hexPrimary = 0xFFF07E4E;
  static const int hexPrimaryDark = 0xFFDF4919;
  static const int hexPrimaryLight = 0xFFFFA979;
  static const int hexAccent = 0xFF3AB4E0;

  // Other
  static const text = Color(0xFF454545);
  static const background = Color(0xFFF9F4F0);
  static const error = Color(0xFFD2494E);
}
